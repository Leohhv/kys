#include <iostream>
#include <string>
int main() {
std::string a= "1234";
std::string b = a.substring(0,2), x= a.begin(), y=a.end();

int ab = std::stoi(b)+std::stoi(a);
std::string c=std::to_string(ab)}

int len=a.length();//4

return 0;
}
